"""Financial intelligence (Phase 3+).

Structured extraction of financial facts (metrics today) stored as deterministic,
validated, citable relational data — kept strictly separate from RAG chunks
(ADR-007). The LLM may assist extraction but is never the source of truth.
"""
